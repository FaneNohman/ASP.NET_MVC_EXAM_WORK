@model IEnumerable<CORE_MVC_EXAM.Models.UserLesson>

@{
    ViewData["Title"] = "Index";
}

<h1>User lesson relations</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Lesson)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Grade)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Attendance)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <a asp-controller="Users" asp-action="Details" asp-route-id="@item.User.UserId"> @Html.DisplayFor(modelItem => item.User.Login)</a>
            </td>
            <td>

                <a asp-controller="Lessons" asp-action="Details" asp-route-id="@item.Lesson.LessonId"> @Html.DisplayFor(modelItem => item.Lesson.LessonId)</a>

            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Grade)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Attendance)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.primarykey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
